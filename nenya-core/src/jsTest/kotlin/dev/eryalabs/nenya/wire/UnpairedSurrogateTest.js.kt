package dev.eryalabs.nenya.wire

/**
 * `UnpairedSurrogateTest` on JavaScript: every test of [PortableUnpairedSurrogateTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableUnpairedSurrogateTest] for why they cannot run here.
 */
class UnpairedSurrogateTest : PortableUnpairedSurrogateTest()
