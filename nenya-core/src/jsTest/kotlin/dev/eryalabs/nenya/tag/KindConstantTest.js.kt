package dev.eryalabs.nenya.tag

/**
 * `KindConstantTest` on JavaScript: every test of [PortableKindConstantTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableKindConstantTest] for why they cannot run here.
 */
class KindConstantTest : PortableKindConstantTest()
