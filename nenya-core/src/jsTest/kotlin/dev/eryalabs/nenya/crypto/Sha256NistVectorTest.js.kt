package dev.eryalabs.nenya.crypto

/**
 * `Sha256NistVectorTest` on JavaScript: every test of [PortableSha256NistVectorTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableSha256NistVectorTest] for why they cannot run here.
 */
class Sha256NistVectorTest : PortableSha256NistVectorTest()
