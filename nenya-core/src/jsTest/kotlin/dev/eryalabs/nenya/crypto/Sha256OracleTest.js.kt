package dev.eryalabs.nenya.crypto

/**
 * `Sha256OracleTest` on JavaScript: every test of [PortableSha256OracleTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableSha256OracleTest] for why they cannot run here.
 */
class Sha256OracleTest : PortableSha256OracleTest()
