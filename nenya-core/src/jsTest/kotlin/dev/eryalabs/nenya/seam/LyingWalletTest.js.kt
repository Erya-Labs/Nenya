package dev.eryalabs.nenya.seam

/**
 * `LyingWalletTest` on JavaScript: every test of [PortableLyingWalletTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableLyingWalletTest] for why they cannot run here.
 */
class LyingWalletTest : PortableLyingWalletTest()
