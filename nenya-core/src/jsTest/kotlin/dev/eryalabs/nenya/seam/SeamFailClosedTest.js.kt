package dev.eryalabs.nenya.seam

/**
 * `SeamFailClosedTest` on JavaScript: every test of [PortableSeamFailClosedTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableSeamFailClosedTest] for why they cannot run here.
 */
class SeamFailClosedTest : PortableSeamFailClosedTest()
