package dev.eryalabs.nenya.listing

/**
 * `ListingStatusTest` on JavaScript: every test of [PortableListingStatusTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableListingStatusTest] for why they cannot run here.
 */
class ListingStatusTest : PortableListingStatusTest()
