package dev.eryalabs.nenya.payment

/**
 * `PaymentEvidenceTest` on JavaScript: every test of [PortablePaymentEvidenceTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortablePaymentEvidenceTest] for why they cannot run here.
 */
class PaymentEvidenceTest : PortablePaymentEvidenceTest()
