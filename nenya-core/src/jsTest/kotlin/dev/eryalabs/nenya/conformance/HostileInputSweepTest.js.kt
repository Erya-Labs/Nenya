package dev.eryalabs.nenya.conformance

/**
 * `HostileInputSweepTest` on JavaScript: every test of [PortableHostileInputSweepTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableHostileInputSweepTest] for why they cannot run here.
 */
class HostileInputSweepTest : PortableHostileInputSweepTest()
