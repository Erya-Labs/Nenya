package dev.eryalabs.nenya.conformance

/**
 * `CapabilitySurfaceTest` on JavaScript: every test of [PortableCapabilitySurfaceTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableCapabilitySurfaceTest] for why they cannot run here.
 */
class CapabilitySurfaceTest : PortableCapabilitySurfaceTest()
