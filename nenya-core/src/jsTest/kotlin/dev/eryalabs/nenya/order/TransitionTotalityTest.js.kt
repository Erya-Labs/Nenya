package dev.eryalabs.nenya.order

/**
 * `TransitionTotalityTest` on JavaScript: every test of [PortableTransitionTotalityTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableTransitionTotalityTest] for why they cannot run here.
 */
class TransitionTotalityTest : PortableTransitionTotalityTest()
