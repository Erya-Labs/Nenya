package dev.eryalabs.nenya.money

/**
 * `MsatTest` on JavaScript: every test of [PortableMsatTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableMsatTest] for why they cannot run here.
 */
class MsatTest : PortableMsatTest()
