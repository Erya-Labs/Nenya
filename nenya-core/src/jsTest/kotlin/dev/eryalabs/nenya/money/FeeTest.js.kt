package dev.eryalabs.nenya.money

/**
 * `FeeTest` on JavaScript: every test of [PortableFeeTest], under the class name the JVM run reports them
 * with. The JVM-only tests are in `src/jvmTest`; see [PortableFeeTest] for why they cannot run here.
 */
class FeeTest : PortableFeeTest()
